 - hosts: all
   become: true
   roles:
     - base
    
 - hosts: all
   become: true
   roles:
     - workstations

 - hosts: all
   become: true

  
 - hosts: all
   become: true
   vars:
     the_user: "{{ marcel }}"
   tasks:
     - name: change user shell to fish 
       become: yes
       user:
         name: "{{ the_user }}"
         shell: /bin/fish
